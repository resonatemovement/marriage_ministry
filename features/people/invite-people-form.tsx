"use client";
import { useState, useTransition } from "react";
import { Plus, Save } from "lucide-react";
import { Sheet, SheetContent, SheetDescription, SheetHeader, SheetTitle } from "@/components/ui/sheet";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { createPeopleInvitations } from "./invitation-actions";
import { invitationSuccessMessage } from "./invitation-messages";
import { isGroupedInvitationRole, type InvitationPerson, validateInvitation } from "./invitation-validation";
type Campus={id:string;name:string}; const empty=():InvitationPerson=>({firstName:"",lastName:"",email:""}); const types=["admin","super_admin","author","couple","coach","counselor"] as const;
export function InvitePeopleForm({campuses,isSuperAdmin}:{campuses:Campus[];isSuperAdmin:boolean}){const[open,setOpen]=useState(false),[role,setRole]=useState("admin"),[campus,setCampus]=useState(""),[tab,setTab]=useState("person-1"),[people,setPeople]=useState<[InvitationPerson,InvitationPerson]>([empty(),empty()]),[submitted,setSubmitted]=useState(false),[serverError,setServerError]=useState<string|null>(null),[success,setSuccess]=useState<string|null>(null),[pending,startTransition]=useTransition();const grouped=isGroupedInvitationRole(role),errors=validateInvitation(role,campus,people),valid=!Object.keys(errors).length,available=types.filter(type=>type!=="super_admin"||isSuperAdmin);const reset=()=>{setRole("admin");setCampus("");setTab("person-1");setPeople([empty(),empty()]);setSubmitted(false);setServerError(null);setSuccess(null)};const update=(index:number,key:keyof InvitationPerson,value:string)=>setPeople(current=>current.map((person,i)=>i===index?{...person,[key]:value}:person) as [InvitationPerson,InvitationPerson]);const submit=()=>{setSubmitted(true);if(!valid){if(Object.keys(errors).some(key=>key.endsWith("1")))setTab("person-2");return}const data=new FormData();data.set("role",role);data.set("campusId",campus);people.slice(0,grouped?2:1).forEach((person,index)=>{data.set(`firstName${index}`,person.firstName);data.set(`lastName${index}`,person.lastName);data.set(`email${index}`,person.email)});startTransition(async()=>{const result=await createPeopleInvitations(data);if("error"in result){setServerError(result.error);return}setOpen(false);setSuccess(invitationSuccessMessage(result))})};return <><button type="button" onClick={()=>{reset();setOpen(true)}} className="inline-flex min-h-10 items-center gap-2 rounded-md bg-brand-primary px-4 py-2 text-sm font-bold text-white"><Plus className="size-4"/>Invite People</button>{success&&<p role="status" className="mt-3 text-sm text-success-strong">{success}</p>}<Sheet open={open} onOpenChange={next=>{if(!next)reset();setOpen(next)}}><SheetContent side="right" className="overflow-y-auto bg-sidebar p-0"><SheetHeader className="border-b border-border px-6 py-5"><SheetTitle>Invite People</SheetTitle><SheetDescription>Create invitations and send secure account emails.</SheetDescription></SheetHeader><form noValidate className="grid gap-5 p-6" onSubmit={event=>{event.preventDefault();submit()}}><label className="grid gap-1.5 text-sm font-medium">Invite type<Select value={role} onValueChange={value=>{setRole(value);setTab("person-1");if(!isGroupedInvitationRole(value))setPeople(current=>[current[0],empty()])}}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent>{available.map(type=><SelectItem key={type} value={type}>{type.replace("_"," ").replace(/\b\w/g,c=>c.toUpperCase())}</SelectItem>)}</SelectContent></Select></label><label className="grid gap-1.5 text-sm font-medium">Campus<Select value={campus} onValueChange={setCampus}><SelectTrigger className={submitted&&errors.campus?"border-error-strong":""}><SelectValue placeholder="Select an active Campus"/></SelectTrigger><SelectContent>{campuses.map(c=><SelectItem key={c.id} value={c.id}>{c.name}</SelectItem>)}</SelectContent></Select>{submitted&&errors.campus&&<Error text={errors.campus}/>}</label>{grouped&&<p className="text-sm text-text-muted">Each person receives a separate account invitation.</p>}{grouped?<Tabs value={tab} onValueChange={setTab}><TabsList className="w-full"><TabsTrigger value="person-1" className="flex-1">First Person</TabsTrigger><TabsTrigger value="person-2" className="flex-1">Second Person</TabsTrigger></TabsList><TabsContent value="person-1"><Person index={0} person={people[0]} errors={errors} submitted={submitted} update={update}/></TabsContent><TabsContent value="person-2"><Person index={1} person={people[1]} errors={errors} submitted={submitted} update={update}/></TabsContent></Tabs>:<Person index={0} person={people[0]} errors={errors} submitted={submitted} update={update} standalone/>}{serverError&&<Error text={serverError}/>}<button disabled={!valid||pending} className="inline-flex min-h-10 items-center justify-center gap-2 rounded-md bg-brand-primary px-4 py-2 text-sm font-bold text-white disabled:cursor-not-allowed disabled:opacity-60"><Save className="size-4"/>{pending?"Sending...":"Send invitation"}</button></form></SheetContent></Sheet></>}
function Person({index,person,errors,submitted,update,standalone}:{index:number;person:InvitationPerson;errors:Record<string,string>;submitted:boolean;update:(i:number,k:keyof InvitationPerson,v:string)=>void;standalone?:boolean}){const title=standalone?"Invitee Details":index?"Second Person Details":"First Person Details";return <section className="grid gap-3 rounded-md border border-border p-4"><h3 className="whitespace-nowrap text-sm font-semibold">{title}</h3>{(["firstName","lastName","email"] as const).map(key=>{const id=`${key}${index}`,error=submitted?errors[id]:undefined;return <div key={id} className="grid gap-1"><input value={person[key]} onChange={e=>update(index,key,e.target.value)} aria-invalid={Boolean(error)} placeholder={key==="firstName"?"First name":key==="lastName"?"Last name":"Email"} className={`min-h-10 rounded-md border bg-white px-3 text-sm ${error?"border-error-strong":"border-border"}`}/>{error&&<Error text={error}/>}</div>})}</section>}
function Error({text}:{text:string}){return <p role="alert" className="text-sm text-error-strong">{text}</p>}
